using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface ITextTimeCalculator 
{
    // Start is called before the first frame update
    float CalculateTime(string text);
            
}
